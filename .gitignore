# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Entorno
.env

# Herramientas
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Node / frontend
node_modules/
dist/

# private
backend/tests/samples/

# Config local de Claude Code (permisos de esta máquina, no del proyecto)
.claude/settings.local.json
# Certificados: los de ARCA en producción llegan a la VM por scp, nunca por git, y los de
# desarrollo (frontend/certs/dev, para que Safari abra la app desde la LAN) los genera cada uno
# con las IP de su red. El patrón sin barra al principio cubre las dos carpetas.
certs/
